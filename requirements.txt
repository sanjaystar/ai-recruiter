# Pinned to the versions the indexes + ranker were built and tested on.
# Relevance is purely dense-embedding based. scipy is a sentence-transformers dependency.
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
sentence-transformers==5.5.1
streamlit>=1.37.0
openpyxl>=3.1.0
